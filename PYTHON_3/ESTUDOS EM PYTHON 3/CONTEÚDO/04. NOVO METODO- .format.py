# O .format é a nova forma de escrever o print, fica mais organizado.

n1 = int(input(' Digite um Numero: '))
n2 = int(input(' Digite outro numero: '))
s = n1 + n2

print('A soma entre {} e {} vale {}'.format(n1, n2, s))

# Colocamos a Frase toda, onde queremos que vá os dados, colocamos conchetes.
# Depois no .format, colocamos em ordem os dados.
